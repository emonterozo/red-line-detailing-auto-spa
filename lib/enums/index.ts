export enum ServiceType {
  SERVICE = 'service', ADD_ONS = 'add-ons', 
}

export enum InquiryStatus {
  NEW = 'new', READ = 'read', COMPLETED = 'completed', REJECTED = 'rejected',
}

export enum BookingStatus {
  FOR_CHECKING = 'for_checking', PENDING_PAYMENT = 'pending_payment', RESERVED = 'reserved', COMPLETED = 'completed', CANCELLED = 'cancelled', REJECTED = 'rejected',
}